# docker-projet-c-arthur
# Band Names Generator

## Prérequis
- Docker & Docker Compose (v2.x recommandé)
- Copiez `.env.dist` en `.env` et remplissez les mots de passe.

```bash
cp .env.dist .env
# Edit .env to set strong passwords